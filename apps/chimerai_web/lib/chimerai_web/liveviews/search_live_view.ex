defmodule ChimeraiWeb.SearchLiveView do
  use Phoenix.LiveView

  # Handles the live search functionality, updating the display as the user types.
  def handle_event("search", %{"query" => query}, socket) do
  end
end
