defmodule ChimeraiWeb.Controllers.ModelController do
  use ChimeraiWeb, :controller

  # Renders the model page for viewing by any user.
  def show(conn, _params) do
  end

  # Renders the model edit page for the model owner.
  def edit(conn, _params) do
  end

  # Handles the model update action.
  def update(conn, _params) do
  end

  # Renders the model upload page.
  def new(conn, _params) do
  end

  # Handles the model upload action.
  def create(conn, _params) do
  end
end
